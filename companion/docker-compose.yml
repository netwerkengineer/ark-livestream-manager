services:
  companion:
    image: "companion-v4-latest:latest"
    container_name: companion
    ports:
      - "8000:8000"
      - "12321:12321/udp"
    volumes:
      - /mnt/data/docker/companion:/companion
    restart: always
